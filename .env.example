VITE_PRIVY_APP_ID=
PRIVY_APP_SECRET=
DATABASE_URL=

# Optional
VITE_API_BASE_URL=
# Defaults to `sepolia` in local dev and `base` in production builds.
VITE_HATS_CHAIN=
# Turn this on for Privy smart wallets and gas sponsorship.
VITE_PREFER_SMART_WALLET_EXECUTION=false

# Pinata artifact deploys
PINATA_JWT=
# Example: https://gateway.daothegame.com
PINATA_GATEWAY_BASE_URL=

# OpenAI worker generation
OPENAI_API_KEY=
# Optional debug dumps for worker generation.
ARTIFACT_DEBUG_WORKERS=false
VITE_ARTIFACT_DEBUG_WORKERS=false
