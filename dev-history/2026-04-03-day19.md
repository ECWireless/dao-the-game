# 2026-04-03 — Day 19

## Sprint 3 Day 5 — Allowlisted registry workers in the RaidGuild roster

- Added `VITE_WORKER_ALLOWLIST` as a local demo-only allowlist keyed by worker `registryKey`.
- Wired the app to fetch registry workers from DAO the Game via manifest-only hydration for roster assembly.
- Kept the playable roster locked to 6 total workers:
  - `ui-designer` replaces Kestrel
  - `brand-designer` replaces Hexa
  - `frontend-engineer` replaces Rune, then Dorian
  - `code-reviewer` replaces Sable, then Mint
- Made whitelist order win within the shared frontend and reviewer buckets.
- Kept built-in workers as silent fallback when an allowlisted worker is unavailable or invalid.
- Deferred `profile.json` loading until the worker avatar/card is opened, instead of loading profile data up front for the roster.
- Added focused tests around the slot replacement logic.
