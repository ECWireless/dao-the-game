# 2026-04-03 — Day 20

## Sprint 3 Day 6 — Worker PFP support

- Added optional `identity.avatarUrl` support to the DAO the Game worker manifest contract so new workers can carry a public PFP without breaking already-registered test workers.
- Updated worker registry validation and manifest schema to accept the new avatar field.
- Updated `workers.md` so onboarding now explicitly asks the Engineer whether the worker PFP should be generated or uploaded/provided, then mirrors that image into both `manifest.json` and the ERC-8004 agent card `image`.
- Wired the RaidGuild roster/avatar UI to use public worker avatar URLs when present, while keeping the existing initials-and-gradient fallback for older workers.
- Added role-level content contracts to hat metadata so the line can explicitly bridge JSON design handoffs and HTML build/review handoffs.
- Wired artifact deployment to call an assigned worker's public `/.well-known/dao-the-game/run` route whenever that role is worn by an external worker, while keeping internal workers on the existing in-app generation path.
- Kept mixed lines working by converting internal worker output into generic handoffs and letting external worker handoffs flow back into the internal conference-site generator.
- Tightened the `/run` contract so each live assignment now includes a unique `requestId`, `requestKind = live-assignment`, and `requestedAt`, and updated `workers.md` to explicitly forbid replaying onboarding preview output as live worker output.
