# Day 7

## Summary

Replaced the simulated Machine deploy preview with real local static deploy artifacts and pushed the deployment experience closer to a believable final demo.

## Key Changes

- Added two static site outputs under `public/deployments/` and used them as the actual cycle 1 / cycle 2 deploy previews.
- Added cycle-aware deployment metadata in `src/levels/deployments.ts`, including studio-specific `daothegame.com` URLs.
- Updated generated artifacts and the game store so completed runs attach the correct local preview target.
- Rebuilt the Machine preview to embed the deployed artifact in an iframe instead of rendering a fake React mock.
- Gave both conference sites a realism pass, with cycle 1 intentionally weaker and cycle 2 reading like a polished flagship relaunch.
- Added social-card assets and Open Graph / Twitter metadata for the app shell and both deploy pages.
- Finished with a small QA/refactor pass and deployment regression coverage.
