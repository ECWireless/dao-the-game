# Day 8

## Summary

Added a gated intro explainer dialog so the demo starts with a short framing beat before the player enters the phone flow.

## Key Changes

- Added a modal intro dialog with demo context and a `Start Demo` action.
- Persisted intro dismissal in the game store so it does not reappear on refresh mid-run.
- Resetting the tutorial now restores the intro dialog.
- Paused initial shell-side startup effects while the dialog is open so the first in-app timing does not start behind the overlay.
- Added Vercel Analytics at the app root using the React package entrypoint.
- Rebuilt the RaidGuild worker avatars with stronger silhouette variation, hand-picked feature presets per member, and a more polished portrait frame.
- Split the avatar renderer into smaller files so the richer portrait system still respects the file-size rule.
